name: Build Changes
on: [push, pull_request]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v2

      - name: Build the mod
        run: mvn package

      - name: Build the mod for Android
        if: runner.os == 'Linux'
        run: |
          echo "$ANDROID_HOME/build-tools/32.0.0" >> $GITHUB_PATH
          ./scripts/package_android.sh -api 32 -game v135.2

      - uses: actions/upload-artifact@v2
        if: runner.os == 'Linux'
        with:
          name: riim
          path: target/riim-*.jar
